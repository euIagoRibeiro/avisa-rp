import { View, Text } from 'react-native';

export function MapTabScreen() {
  return (
    <View className="flex-1 items-center justify-center bg-white">
      <Text className="text-gray-400 text-base">Mapa (em breve)</Text>
    </View>
  );
}
